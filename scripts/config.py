# config.py

DB_CONFIG = {
    "dbname": "seu_banco",      # Nome do banco de dados
    "user": "seu_usuario",      # Seu usuário PostgreSQL
    "password": "sua_senha",    # Sua senha PostgreSQL
    "host": "localhost",        # Host do banco de dados (pode ser um IP ou domínio)
    "port": "5432"              # Porta do PostgreSQL
}
